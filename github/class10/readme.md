# create one group in aws console named as thirdparty and added few users with only upload & download permissions to s3 bucket permissions
# list all users in that group using paginator if the user is more than 1000
# using FOR loop . looping into users meta data & def functions get access data
